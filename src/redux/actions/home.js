
const getAllPokemons = (page, limit) => {
  console.log("inside getAllPokemons ===> ", page, limit);
  return async (dispatch) => {
    try {
      // const respMyTodaysTasks = await ApiService.getAllPokemons();
    }
    catch(e) {
    }
  };
}

export {
  getAllPokemons,
};